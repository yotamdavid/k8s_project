apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  selector:
    app: yotamdavid-ferari-website
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 32000

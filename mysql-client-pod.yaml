apiVersion: v1
kind: Pod
metadata:
  name: mysql-test-pod
spec:
  containers:
    - name: mysql-client
      image: yotamdavid/appdatabase:latest
      command: ['sh', '-c', 'while true; do mysql -h db -u root -pyotam -e "SHOW DATABASES"; sleep 10; done']

apiVersion: apps/v1
kind: Deployment
metadata:
  name: docker-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: docker-app
  template:
    metadata:
      labels:
        app: docker-app
    spec:
      containers:
        - name: nginx-container
          image: yotamdavid/ferari_website:1.0
          ports:
            - containerPort: 5000
